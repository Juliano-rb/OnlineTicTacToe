# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: deploy to frontend and backend on merge
'on':
  push:
    branches:
      - main
env:
  FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
  NODE_ENV: 'production'
  DISABLE_ESLINT_PLUGIN: 'true'
jobs:
  deploy_back_end:
    name: deploy server to fly.io
    defaults:
      run:
        working-directory: server
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - run: flyctl deploy --remote-only -a jogarvelha --build-arg ENVIRONMENT=production
  deploy_front_end:
    name: deploy frontend to firebase hosting
    defaults:
      run:
        working-directory: frontend
    runs-on: ubuntu-latest
    env:
      REACT_APP_SERVER_URL: https://jogarvelha.fly.dev/
      HUSKY: 0
    steps:
      - uses: actions/checkout@v2
      - run: yarn ci
      - run: yarn build
      - run: yarn test
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entryPoint: "./frontend"
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_JOGARVELHA }}'
          channelId: live
          projectId: jogarvelha
